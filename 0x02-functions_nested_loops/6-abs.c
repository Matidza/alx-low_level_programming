#include "main.h"
/**
 * main - entry point compute the
 * Description: int type number
 * Return: absolute value of @n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
	return (0);
}
